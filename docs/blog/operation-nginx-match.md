# nginx 匹配规则

```js
Nginx 的 location 实现了对请求的细分处理，有些 URI 返回静态内容，有些分发到后端服务器等，今天来彻底弄懂它的匹配规则

Nginx location 的配置

-语法规则
 location [ = | ~ | ~* | ^~ | 空 ] url { … }

-修饰符
= 表示精准匹配。完全相等时，才会命中规则。
~ 表示区分大小写的正则匹配。
~* 表示不区分大小写的正则匹配。
^~ 表示最佳匹配，即命中后采用该规则，不再进行后续的查找。

-匹配过程
精准匹配
最佳匹配
正则匹配
最长匹配前缀字符串
```
